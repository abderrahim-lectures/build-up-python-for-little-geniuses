"""Chapter 8: Functions. A math formula like <code>area = length * width</code> takes inputs and produces an output – a Python function is exactly that: input in, answer out."""


def area(length: float, width: float):
    """Return the area of a rectangle."""
    return length * width
